class MediDuration {
  static const Duration defaultDuration = Duration(seconds: 2);
  static const Duration slideTextDuration = Duration(seconds: 1);
  static const Duration chatBotDuration = Duration(seconds: 3);
  static const Duration diseaseDuration = Duration(seconds: 3);
  static const Duration showCaseDuration = Duration(seconds: 3);
}
